import { BufferGeometry, ConeGeometry, CylinderGeometry, EllipseCurve, Group, Line, LineBasicMaterial, Mesh, MeshPhongMaterial, SphereGeometry } from 'three';

export default class Mailbox extends Group {
    constructor () {
        super();

        
    }
}
